# /api/characters

get:
  tags:
    - 'Characters'
  summary: 'Get all characters'
  description: ''
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: '../../schemas/characters.yaml#/CharacterList'
    401:
      $ref: '../../schemas/global.yaml#/Error'
    500:
      $ref: '../../schemas/global.yaml#/Error'
