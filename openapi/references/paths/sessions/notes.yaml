# /api/sessions/{id}/notes

get:
  tags:
    - 'Sessions'
  summary: 'Get session notes'
  description: 'Get session notes for the current authenticated user'
  parameters:
    - name: 'sessionId'
      required: true
      in: 'path'
      schema:
        type : 'number'
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: '../../schemas/sessions.yaml#/Notes'
    400:
      $ref: '../../schemas/global.yaml#/Error'
    401:
      $ref: '../../schemas/global.yaml#/Error'
    404:
      $ref: '../../schemas/global.yaml#/Error'
    500:
      $ref: '../../schemas/global.yaml#/Error'

post:
  tags:
    - 'Sessions'
  summary: 'Update session notes'
  description: 'Update session notes for the current authenticated user'
  parameters:
    - name: 'sessionId'
      required: true
      in: 'path'
      schema:
        type : 'number'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../schemas/sessions.yaml#/NotesEditBody'
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: '../../schemas/sessions.yaml#/Notes'
    400:
      $ref: '../../schemas/global.yaml#/Error'
    401:
      $ref: '../../schemas/global.yaml#/Error'
    404:
      $ref: '../../schemas/global.yaml#/Error'
    500:
      $ref: '../../schemas/global.yaml#/Error'
