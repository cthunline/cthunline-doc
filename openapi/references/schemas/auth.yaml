# Authentication

AuthLoginBody:
    type: 'object'
    properties:
        email:
            type: 'string'
            format: 'email'
            description: 'User email'
        password:
            type: 'string'
            description: 'User password'
    required:
        - email
        - password
