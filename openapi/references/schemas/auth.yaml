# Authentication

LoginBody:
  type: 'object'
  properties:
    email:
      type: 'string'
      format: 'email'
      description: "User email"
    password:
      type: 'string'
      description: "User password"
  example:
    username: 'lucie.bronze@example.com'
    password: 'password123'
